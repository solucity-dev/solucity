# === Dependencias / gestores ===
node_modules/
.pnpm-store/
pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# === Expo / React Native ===
.expo/
.expo-shared/
.expo-web/
dist/
web-build/
expo-env.d.ts

# === Metro bundler cache ===
.metro-cache/
.metro-health-check*

# === Nativo generado (workflow gestionado) ===
android/
ios/
.kotlin/

# === Claves / credenciales ===
*.keystore
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.pem

# === Variables de entorno ===
.env
.env.*
apps/**/.env
apps/**/.env.*
!apps/**/.env.example
.env*.local

# === TypeScript ===
*.tsbuildinfo

# === SO / editores ===
.DS_Store
Thumbs.db
.vscode/

# === Reportes / temporales ===
coverage/
*.log
*.tmp

# === Monorepo específico ===
apps/*/dist
packages/*/dist
build/

# Firebase / Expo secrets
keys/
firebase-admin*.json

# === Backend uploads (CRÍTICO: NUNCA a git) ===
apps/backend/uploads/
apps/backend/uploads/**

# === Prisma local (por si existiera) ===
prisma/dev.db
prisma/dev.db-journal

# root app.json (evitar conflicto con Expo config en apps/mobile)
app.json
