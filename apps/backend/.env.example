# ====== Básicos del servidor ======
# Puerto donde escucha Express
PORT=3000

# Entorno de ejecución: development | production | test
NODE_ENV=development

# Orígenes permitidos para CORS. Lista separada por comas.
# Ejemplos útiles durante el dev (Expo y local):
#  - http://localhost:19006  -> Expo web
#  - http://192.168.0.111:19006 -> tu IP LAN + puerto Expo web
#  - http://localhost:3000   -> front local (si hubiera)
CORS_ORIGIN=http://localhost:19006,http://localhost:3000

# ====== Base de datos (PostgreSQL) ======
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DB_NAME?schema=public
# Cambia USER, PASSWORD, HOST y DB_NAME según tu entorno.
DATABASE_URL=postgresql://postgres:211442@localhost:5432/solucity?schema=public

# ====== Seguridad / Rate limiting ======
# Ventana de tiempo para el rate limit (en ms). Ej: 15min = 900000
RATE_LIMIT_WINDOW_MS=900000
# Máximo de solicitudes por IP dentro de la ventana
RATE_LIMIT_MAX=100

# ====== Logs ======
# Nivel de log para morgan: dev | combined
LOG_FORMAT=dev
